<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder"
	xmlns:g="urn:import:com.google.gwt.user.client.ui">
	<ui:style src="../common.css">
		.jsonHeaderCaption{
			font-weight:bold;
		}
		.addForm{ 
			margin-top: 15px;
		}
		.connectedInfo{
			color: green;
			font-weight: bold;
		}
		.storeDataPanel{
			display: none;
		}
		.shareUrlPanel{
			display: none;
		}
		.shareLink{}
	</ui:style>
	<g:HTMLPanel styleName="{style.subpageContainer}">
		<g:Button styleName="{style.subpageCloseButton}" ui:field="closeButton" text="x"></g:Button>
		
		<h1 class="{style.contentTitle} {style.about}">Import/Export options</h1>
		
		<p>
			Unfortunetly HTML does not allow to save files outside browser filesystem.<br/>
			Application give possibility to save Your saved requests on applications server (Google App Engine). 
		</p>
		<section>
		<p>
			First step is to connect with server.<br/>
			When you install application on other machine you will be able to download all data.
		</p>
		<p>
			<g:Button ui:field="connectButton" text="Connect"></g:Button>
			<g:InlineLabel styleName="{style.connectedInfo}" ui:field="loggedInInfo" text="You are already connected" visible="false"></g:InlineLabel>
			<g:InlineLabel styleName="{style.connectedInfo}" ui:field="statusInfo" text=""></g:InlineLabel>
		</p>
		<p ui:field="connectNote" class="{style.note}">
			This actually doesn't do nothing.<br/>
			Requests are not automatically synchronized with server. Still you need press "Export" button. 
		</p>
		
		</section>
		<section ui:field="storeDataPanel" class="{style.storeDataPanel}">
			<p>
				Now you can store or restore your data.<br/>
			</p>
			<p>
				<g:Button ui:field="storeData" text="Store current data on external server"></g:Button>
				<g:Button ui:field="restoreData" text="Restore my data"></g:Button>
			</p>
		</section>
		<section ui:field="shareUrlPanel" class="{style.shareUrlPanel}">
			<p>
				You can share all your saved requests data by giving someone link below
			</p>
			<pre class="{style.shareLink}" ui:field="shareLink">
				
			</pre>
		</section>
	</g:HTMLPanel>
</ui:UiBinder> 